import styled from 'styled-components';

export const CalendarContainer = styled.div`
  display: flex;
  justify-content: center;
`;

export const CalendarTitle = styled.h4`
  margin: 10px 0px;

  font-size: 1.5rem;
  text-align: center;
`;

export const CalendarBody = styled.h4`
  margin: 0;

  font-size: 0.9rem;
  text-align: center;

  line-height: 1.5rem;
`;
